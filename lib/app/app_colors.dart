import 'package:flutter/material.dart';

class AppColors {
  const AppColors._();

  static const background = Color(0xFF050505);
  static const surface = Color(0xFF121212);
  static const surfaceHigh = Color(0xFF1B1B1B);
  static const card = Color(0xFF202020);
  static const cardActive = Color(0xFF2A0E0E);
  static const primary = Color(0xFFE32222);
  static const primaryStrong = Color(0xFFFF2A2A);
  static const onPrimary = Colors.white;
  static const text = Colors.white;
  static const textMuted = Color(0xFF9A9A9A);
  static const textFaint = Color(0xFF525252);
  static const progressTrack = Color(0xFF2B2B2B);
  static const gold = Color(0xFFFFC857);
}
